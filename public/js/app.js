/**
 * Primero, cargamos todas las dependencias de JavaScript de este proyecto,
 * que incluye Bootstrap.
 */

import './bootstrap';

/**
 * Opcionalmente, puedes importar Alpine.js si lo usas para interactividad.
 * import Alpine from 'alpinejs';
 * window.Alpine = Alpine;
 * Alpine.start();
 */